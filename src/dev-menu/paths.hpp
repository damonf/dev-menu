#ifndef PATHS_HPP
#define PATHS_HPP

#include <string>
#include <vector>

namespace dev_menu {

std::vector<std::string> get_paths(const std::string& dir);

} // namespace dev_menu

#endif // PATHS_HPP
